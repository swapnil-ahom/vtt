export enum status_enum {
    read = 1,
    unread = 0
}

export enum user_type_enum {
    SUBSCRIBER = 1,
    SUPERADMIN = 2,
    ADMIN = 3,
    TRAINER = 4,
    COORDINATOR = 5,
    TRAINEE = 6,
    ANONYMOUS = 7
}