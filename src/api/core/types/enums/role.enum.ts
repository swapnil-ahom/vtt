/**
 * @description Define supported roles
 */
export enum ROLE {
  admin = 'admin',
  user = 'user',
  ghost = 'ghost',
  superAdmin = 'superAdmin',
  subscriber = 'subscriber',
  undecided = 'undecided'
}

export enum RoleIds {
  superAdmin = 1,
  admin = 2,
  subscriber = 3,
  coordinator = 4,
  undecided = 5,
  trainer = 6,
  trainee = 7
}
